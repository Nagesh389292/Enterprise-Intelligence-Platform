{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd control_tower && npm install && npm run build",
  "outputDirectory": "control_tower/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
